"""Package entry point.

Re-export the public API here, so callers can `from <package> import X` rather
than reaching into modules:

    from .some_module import some_function

Relative imports, so this survives renaming the package folder. Nothing is
exported yet — the example script has no public API to expose.
"""
